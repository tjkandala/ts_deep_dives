/**
 * Building Git
 *
 * TODO: Configure tests. Mock the filesystem
 *
 * Don't forget to (somehow) use Worker Threads and Child Processes!
 */

const path = process.cwd();

console.log(path);

console.log(process.argv);
