describe("lexer", () => {
  test("works", () => {
    const name = "++";

    const char = name.charCodeAt(1);

    console.log(char);

    console.log("teej".slice(1, 2));

    expect(true).toBe(true);
  });
});
